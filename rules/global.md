# Global Rules

Always-loaded rules for every Claude Code session. Keep each rule short; when a rule
has a detailed procedure behind it, point at the matching skill instead of inlining it.

## Tooling

- Python packaging/envs: always uv (`uv add`, `uv run`, `uvx`), never pip/poetry/pipenv.
  Detailed workflows: uv skill.

## Workflow

<!-- Rules about how to approach work: branching, testing, review habits. -->

## Style

<!-- Rules about code and communication style. -->
